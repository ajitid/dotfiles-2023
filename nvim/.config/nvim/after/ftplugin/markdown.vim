setlocal wrap linebreak
" Treesitter should supposedly handle this
" let g:markdown_fenced_languages = ['go', 'sh', 'bash=sh', 'html', 'css', 'javascript', 'js=javascript', 'json', 'typescript', 'ts=typescript', 'perl', 'python', 'ruby', 'xml']

setlocal foldmethod=indent
" To fold all do `setlocal foldlevel=0` and to unfold all do `setlocal foldlevel=21`.
" To unfold you can also do `zO` or use `l` key manually on a fold to unfold it.
